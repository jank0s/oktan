<!DOCTYPE html>
<html>
<head>
	<title>Oktan - spletna aplikacija za beleženje in analizo porabe</title>
	<%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
	<%= javascript_include_tag "application", "data-turbolinks-track" => true %>
	<%= csrf_meta_tags %>
<head>
<body>
	<div class="container">
		<header>
			<div class="inner">
				<div class="logo" title="Oktan"><%= image_tag("logo.png", alt: "logo") %></div>
				<div id="login">
					<% if signed_in? %>
						<%= current_user.email %> | <%= link_to "Odjava", signout_path, method: "delete" %>
					<% else %>
						<a href="/" id="miLogin">Prijava</a> | <a href="/users/new" id="miRegister">Registracija</a>
					<% end %>
				</div>
			</div>
		</header>
		<nav id="nav-bar">
			<div class="inner">
				<ul class="nav-bar">
					<li><%= link_to "Domov", root_path %></li>
					<li><%= link_to "Iskanje", cars_search_path %></li>
					<% if signed_in? %>
						<li><%= link_to "Moj profil", current_user %></li>
					<% end %>
				</ul>
				<ul class="lang-bar">
				</ul>
			</div>
		</nav>
		<div id="content">
			<%= yield %>
		</div>
		<footer>
			<div class="inner">Developed by JK</div>
		</footer>
	</div>
</body>
</html>